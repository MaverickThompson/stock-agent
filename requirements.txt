# Core. Versions are floors, not pins -- the code targets current APIs and is
# verified against numpy 2.4, pandas 3.0, scipy 1.17, hmmlearn 0.3.3.
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3
hmmlearn>=0.3.0

# Data download (scripts/fetch_data.py uses only this plus the stdlib).
requests>=2.31

# Optional: charts for backtest output.
matplotlib>=3.8

# Broker and market data for the 60-day study. Section 7 names an Alpaca
# paper account with broker-side order records as the intended venue.
alpaca-py>=0.33

# Error alerting for unattended sessions. A session that dies quietly puts a
# hole in the study that has to be disclosed; see src/stockagent/observability.py.
sentry-sdk>=2.0

# Development.
pytest>=7.4
