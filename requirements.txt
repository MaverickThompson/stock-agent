# Core. Versions are floors, not pins -- the code targets current APIs and is
# verified against numpy 2.4, pandas 3.0, scipy 1.17, hmmlearn 0.3.3.
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.3
hmmlearn>=0.3.0

# Data download (scripts/fetch_data.py uses only this plus the stdlib).
requests>=2.31

# Optional: charts for backtest output.
matplotlib>=3.8

# Development.
pytest>=7.4
